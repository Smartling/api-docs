definitions:
  SuccessResponse:
    type: object
    required:
      - code
    properties:
      code:
        type: string
        enum:
          - SUCCESS
        description: Indicates whether the response was successful or what error has occured.
