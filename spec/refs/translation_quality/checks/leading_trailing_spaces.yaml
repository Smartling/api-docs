definitions:
  account_check_types_LEADING_TRAILING_SPACES_settings_effective:
    get:
      description: >-
        Get checktype LEADING_TRAILING_SPACES settings for certain account.
      summary: Get checktype LEADING_TRAILING_SPACES settings for certain account
      tags:
        - Translation quality check
      consumes:
        - application/json
      produces:
        - application/json
      parameters:
        - $ref: '../../common.yaml#/definitions/pathParameters/accountUid'
        - $ref: '../common.yaml#/definitions/requestQueryParameter/targetLocaleId'

      responses:
        200:
          description: OK
          schema:
            $ref: '#/definitions/AccountCheckTypeEffectiveResponse'

      x-code-samples:
        - lang: curl
          source: |
            curl -H "Authorization: Bearer $smartlingToken" -H "Content-Type: application/json" https://api.smartling.com/translation-quality-api/v2/accounts/$smartlingAccountId/check-types/LEADING_TRAILING_SPACES:

  AccountCheckTypeEffectiveResponse:
    allOf:
      - $ref: '/spec/refs/common.yaml#/definitions/SuccessResponse'
      - type: object
        properties:
          data:
            allOf:
              - $ref: '../common.yaml#/definitions/responseObjects/severityLevelCodeResponse'
              - $ref: '../common.yaml#/definitions/accountCheckTypeSettingsEffectiveResponse/emptySubTypes'
              - $ref: '../common.yaml#/definitions/accountCheckTypeSettingsEffectiveResponse/localeSeverityLevels'
              - $ref: '../common.yaml#/definitions/accountCheckTypeSettingsEffectiveResponse/groups'
              - type: object
                properties:
                  checkTypeCode:
                    type: string
                    enum:
                      - LEADING_TRAILING_SPACES
                    description: check type code
                    example: "LEADING_TRAILING_SPACES"

